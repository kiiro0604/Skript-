on rightclick on player:
    if name of tool of player is "&6cowboy":
        make the player ride clicked entity

command /ride [<player>]:
    trigger:
        if arg 1 is not set:
            give saddle named "&6cowboy" to player
            stop
        if arg 1 is set:
            give saddle named "&6cowboy" to arg 1
            stop